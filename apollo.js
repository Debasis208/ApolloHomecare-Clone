const menubar = document.querySelector(".menu_bar_section");
const menulogo = document.querySelector(".menu_section");

menulogo.addEventListener("click",()=>{
    menulogo.classList.toggle("menubar")
}

)